# Generated by tools/generate_mission.py -- DO NOT hand-edit the vms/
# placement sections; re-run the generator instead so they stay
# consistent with each other and with config/hosts.yaml. See
# management/mission_defs/README.md for the full schema reference
# and management/mission_defs/template.yaml for an annotated example.
#
# file_version: 1.0.0 -- this mission file's own revision,
# recorded in deployment status/step logs for audit purposes.
#
# Summary: 4 VMs (2 linked-clone, 2 PXE), 1 with GPU passthrough, 24 total NICs.
# Requested: 18 vCPU, 64 GiB memory.
#
# IMPORTANT -- every interface's mac_suffix below is an ARBITRARY
# PLACEHOLDER, not a generated/derived value. Per operator
# confirmation, guest software running in these VMs has MAC
# addresses hardcoded into its licensing/configuration, so the real
# suffixes are a manual input the operator is responsible for.
# Before deploying against real hardware, review every VM's
# interfaces: below and replace these placeholders with the actual
# required values. The first three octets of every MAC are NOT
# configured here at all -- a fresh random prefix is generated for
# each individual deployment of this file at registration time (see
# core/macs.py), so this same file can be deployed more than once
# concurrently, fully isolated at the MAC layer, while your
# suffix-based substring matching keeps working inside each
# deployment's own isolated network.
mission: Mission-Bravo
file_version: 1.0.0
networks:
  control: 100
  storage: 110
  operations: 120
  sensor: 130
  external: 140
  management: 150
vms:
  db01:
    type: linked_clone
    cpu: 4
    memory: 16384
    interfaces:
      control:
        mac_suffix: '10:01:00'
      storage:
        mac_suffix: '10:01:01'
      operations:
        mac_suffix: '10:01:02'
      sensor:
        mac_suffix: '10:01:03'
      external:
        mac_suffix: '10:01:04'
      management:
        mac_suffix: '10:01:05'
    image: rhel9-db-golden
    image_revision: Alpha
  app01:
    type: linked_clone
    cpu: 2
    memory: 8192
    interfaces:
      control:
        mac_suffix: '10:00:00'
      storage:
        mac_suffix: '10:00:01'
      operations:
        mac_suffix: '10:00:02'
      sensor:
        mac_suffix: '10:00:03'
      external:
        mac_suffix: '10:00:04'
      management:
        mac_suffix: '10:00:05'
    image: rhel9-app-golden
    image_revision: Alpha
  render01:
    type: pxe
    cpu: 8
    memory: 32768
    interfaces:
      control:
        mac_suffix: '10:02:00'
      storage:
        mac_suffix: '10:02:01'
      operations:
        mac_suffix: '10:02:02'
      sensor:
        mac_suffix: '10:02:03'
      external:
        mac_suffix: '10:02:04'
      management:
        mac_suffix: '10:02:05'
    gpu:
      profile: H100-MIG-3g.40gb
  worker01:
    type: pxe
    cpu: 4
    memory: 8192
    interfaces:
      control:
        mac_suffix: '10:03:00'
      storage:
        mac_suffix: '10:03:01'
      operations:
        mac_suffix: '10:03:02'
      sensor:
        mac_suffix: '10:03:03'
      external:
        mac_suffix: '10:03:04'
      management:
        mac_suffix: '10:03:05'
placement:
  render01: compute01
  db01: compute02
  worker01: compute03
  app01: compute04
